const DetailsImage = ({ images }) => {
  console.log(images);

  let tempImage = "http://pets-images.dev-apis.com/pets/none.jpg";

  //const {chosenImage, changeImage} = {!images.length ? (tempImage) : (images[0])}
};
